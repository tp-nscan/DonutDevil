namespace MathLib.Test.NumericTypes.ModBits
{
    //[TestClass]
    //public class MmapFixture
    //{
    //    [TestMethod]
    //    public void TestMmap()
    //    {
    //        Func<double, double> id = x => x;

    //        var map = Mmap.Make<M4>(id);

    //        foreach (var ival in M.Values<M4>())
    //        {
    //            Assert.AreEqual(ival, map.Invoke(ival));
    //        }
    //    }

    //    [TestMethod]
    //    public void TestMmapToPoint2D()
    //    {
    //        Func<double, double> f = x => Math.Tanh(10 * (x - 0.5)) / 2;

    //        var map = Mmap.Make<M8>(f);

    //        System.Diagnostics.Debug.Print
    //            (
    //                map.ToMapPoints().Aggregate(
    //                "",
    //                (p, pt) => p + "\n" +
    //                    pt.X.ToString("0.0000") + ", " +
    //                    pt.Y.ToString("0.0000")
    //                )
    //            );
    //    }



   // }
}
