<UserControl x:Class="La.View.EntityDataControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:La.View"
             xmlns:common="clr-namespace:La.ViewModel.Design"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance {x:Type common:DesignEntityDataVm}, IsDesignTimeCreatable=True}"
             Background="#2FFF"
             d:DesignWidth="300">
    <Grid>

        <StackPanel>
            <StackPanel Margin="0,0,20,0" Orientation="Horizontal">
                <TextBlock Foreground="Beige" Text="Epn:" Margin="0,0,5,0" VerticalAlignment="Center" />
                <TextBlock FontSize="18" Text="{Binding Epn}" Foreground="Beige"/>
            </StackPanel>
            <StackPanel Margin="0,0,20,0" Orientation="Horizontal">
                <TextBlock Foreground="Beige" Text="Descr:" Margin="0,0,5,0" VerticalAlignment="Center" />
                <TextBlock FontSize="18" Text="{Binding Description}" Foreground="Beige"/>
            </StackPanel>
        </StackPanel>

    </Grid>
</UserControl>
